<h1>FastPasses for <%= @attraction.name %></h1>

<p>Below are the Reserved FastPasses for <%= @attraction.name %> today, <%= Time.now.strftime("%A, %B %d, %Y") %>.
  To use a FastPass, click "View" next to the desired FastPass and then click "USE" on the next page.
</p>

<table class="table">
  <tr>
    <th>Ticket Number</th>
    <th>Customer</th>
    <th>Hour</th>
    <th>Used?</th>
    <th></th>
  </tr>

<% @attfastpasses.each do |fp| %>
  <tr>
    <td><%= fp.id %></td>
    <td><%= Customer.find(fp.cust_id).name %></td>
    <td><%= Time.utc(2010, "jan", 1, fp.hour, 0, 0).strftime("%I:%M %p") %></td>
    <td>
      <% if fp.is_used==1 %>
        Used
      <% else %>
        No
      <% end %>
    </td>
    <td><%= link_to 'View', fp %></td>
  </tr>
<% end %>
</table>
