<h1>Today's Reserved FastPasses</h1>

<p>Below are the FastPasses for today. To see FastPasses for a particular attraction, please visit the attraction's page.
  To use a FastPass, click "View" next to the desired FastPass and then click "USE" on the next page.
</p>


<table class="table">
  <tr>
    <th>Ticket Number</th>
    <th>Attraction</th>
    <th>Customer</th>
    <th>Hour</th>
    <th>Used?</th>
    <th></th>
  </tr>

<% @fastpasses.each do |fastpass| %>
  <tr>
    <td><%= fastpass.id %></td>
    <td><%= Attraction.find(fastpass.attractions_id).name %></td>
    <td><%= Customer.find(fastpass.cust_id).name %></td>
    <td><%= Time.utc(2010, "jan", 1, fastpass.hour, 0, 0).strftime("%I:%M %p") %></td>
    <td>
      <% if fastpass.is_used==1 %>
        Used
      <% else %>
        No
      <% end %>
    </td>
    <td><%= link_to 'View', fastpass %></td>
  </tr>
<% end %>
</table>
