<%= form_for(@fastpass) do |f| %>
  <% if @fastpass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fastpass.errors.count, "error") %> prohibited this fastpass from being saved:</h2>

      <ul>
      <% @fastpass.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attractions_id %><br />
    <%= f.text_field :attractions_id %>
  </div>
  <div class="field">
    <%= f.label :cust_id %><br />
    <%= f.text_field :cust_id %>
  </div>
  <div class="field">
    <%= f.label :hour %><br />
    <%= f.text_field :hour %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
